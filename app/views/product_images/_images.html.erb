<% for image in @product.images %>
  <div style="float:left" id="product_image_<%= image.id %>">
    <span><%= image_tag(image.image.url(:thumb), :width => "100", :height => "90") %></span>
    <%= link_to image_tag("closeX.gif"),product_product_image_path(@product, product_image), :remote => true, :method => 'DELETE' ,:confirm => 'are you sure?' %>
  </div>
<% end %>